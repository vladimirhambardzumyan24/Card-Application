export enum CardValues {
    MAX=1000
} 

export enum ButtonTexts {
    ADD_CARD = 'ADD Card',
    SORT_CARDS = 'Sort Cards'
} 

export enum FooternTexts {
    FOOTER = 'Footer'
} 

export enum RightSideTexts {
    INSTRUCTIONS = 'Press the "ADD Card" button to add the new Card. Use the "Sort Cards" button to sort the Cards by the increase. Press an X icon on the top right to delete them.'
}